pipeline{
    agent any
    stages {
        stage("build") {
            steps{
                sh "mvn install"
            }
        }
        stage("deploy"){
            steps{
                sh "docker-compose up"
            }
        }
    }
}